# TODO

- [x] Create a formal dev-level UI redesign plan for the React/Flask task manager
- [x] Update `frontend/style.css` with design tokens (CSS variables), improved button/form/card styles, and skeleton/empty/error states
- [x] Rewrite `frontend/index.html` to a clean validated React (Babel) structure while preserving logic and UI upgrades
- [x] Add skeleton loaders for tasks/comments during loading
- [x] Add dismissible-style alert UX and accessibility attributes (`role="alert"`, `aria-live`)
- [x] Display `created_at` timestamps for tasks/comments
- [x] Remove inline styles where possible (e.g., icon button sizing)
- [ ] Run manual verification (CRUD tasks/comments + filters + loading/error states)

>>>>>>> fc4a134 (Chore: update TODO after UI work - 23-05-2026)

=======
# TODO

- [x] Create a formal dev-level UI redesign plan for the React/Flask task manager
- [x] Update `frontend/style.css` with design tokens (CSS variables), improved button/form/card styles, and skeleton/empty/error states
- [x] Rewrite `frontend/index.html` to a clean validated React (Babel) structure while preserving logic and UI upgrades
- [x] Add skeleton loaders for tasks/comments during loading
- [x] Add dismissible-style alert UX and accessibility attributes (`role="alert"`, `aria-live`)
- [x] Display `created_at` timestamps for tasks/comments
- [x] Remove inline styles where possible (e.g., icon button sizing)
- [ ] Run manual verification (CRUD tasks/comments + filters + loading/error states)

- [x] Create LICENSE file with MIT license text


